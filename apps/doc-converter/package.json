{
  "name": "@chronos/doc-converter",
  "version": "0.1.0",
  "private": true,
  "description": "Convert documents between formats (Word ↔ PDF, PDF → Audio, etc.)",
  "scripts": {
    "dev": "npx serve . --listen 3001",
    "build": "echo \"Build complete\""
  }
}
